#!/usr/bin/env bash

# Fetches the k8s config from the k3s server and installs it to the set location

set -euo pipefail

dir=$(dirname "$0")
scp -q root@ubuntu-test:/etc/rancher/k3s/k3s.yaml "$dir"/../k3s.yaml
sed -e 's/server: .*/server: https:\/\/ubuntu-test:6443/' -i '' k3s.yaml
