apiVersion: v1
kind: Service
metadata:
  name: openclaw
  labels:
    app: openclaw
spec:
  type: ClusterIP
  ports:
    - port: 18789
      targetPort: gateway
      name: gateway
  selector:
    app: openclaw
